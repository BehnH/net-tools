export type SiteConfig = typeof siteConfig

export const siteConfig = {
  name: "Cert Search",
  description:
    "crt.sh, but make it ✨ actually work ✨",
  mainNav: [
    {
      title: "Home",
      href: "/",
    },
  ],
}
